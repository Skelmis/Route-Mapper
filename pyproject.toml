[project]
name = "route-mapper"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "code-ast>=0.1.2",
    "tree-sitter-c-sharp>=0.23.1",
]

[dependency-groups]
dev = [
    "black>=26.3.0",
]
